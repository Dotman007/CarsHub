using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace CarsHub.Models
{
    public class OrderActivity
    {
        private ApplicationDbContext db = new ApplicationDbContext();
        //public void ApproveOrders(int? carId)
        //{
        //    Car order = db.Cars.Find(carId);
        //    if (order != null)
        //    {
        //        order.Book = false;
        //        db.SaveChanges();
        //    }
        //}
    }
}